export function renderPlants(plant) {
    const plantScienceEl = document.createElement('div');
    const plantCommonEl = document.createElement('p');
    const plantGenderEl = document.createElement('p');
    nameEl.textContent = plant.name;
    img.src = cat.image_url;

    plantEl.classList.add('allPlants');

    plantEl.append(nameEl, img);

    return catEl;
}
//finish this up and edit render function//
